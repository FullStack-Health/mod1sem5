let idade = parseInt(prompt("Qual sua idade?"));

console.log(idade >= 18 ? "Adulto" : "Menor de idade");

var maioridade;

// if (idade >= 18) {
//     maioridade = true;
// } else {
//     maioridade = false
// }

maioridade = idade >= 18;
